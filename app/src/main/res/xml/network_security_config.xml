<?xml version="1.0" encoding="utf-8"?>
<!--
  网络安全配置。
  ============================================================================
  为什么允许明文 HTTP
  ============================================================================
  Android 9（API 28）起，targetSdk >= 28 的应用默认**禁止明文 HTTP**。

  本应用需要连两类端点：
    - 本地推理服务（Ollama 等），几乎都是 http://127.0.0.1:11434；
    - **第三方中转站**，其中相当一部分只提供 http 域名。

  这两类都被默认策略挡死，而用户看到的只会是一句语焉不详的网络错误。
  「能连上用户自己填的端点」在这个应用里是功能本身，不是可选项，因此这里放开明文。

  代价说清楚：走 http 时 API Key 在链路上是明文的，同一局域网内的其他人可能截获。
  所以这一层之外还做了两件事：
    1. Base URL 输入框下方会在检测到 `http://` 时直接提示「Key 将以明文传输」，
       让用户在填写的那一刻就知道代价，而不是事后才发现；
    2. 云端厂商（DeepSeek / OpenAI 等）的预置地址全部是 https。

  这是「可用性 vs 传输安全」之间一次明确的取舍，选择权交给用户，但绝不隐瞒。
  ============================================================================
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
